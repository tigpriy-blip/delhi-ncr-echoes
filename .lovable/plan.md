# Plan

## 1. Import the uploaded project
- Extract `puzzle-playground-a8bbb822-main.zip` into `/dev-server/` with `rsync --exclude='.git'` (verified no `.git` inside).
- This overlays the full puzzle-playground app (routes, components, data, hooks, etc.) on top of the current blank starter.

## 2. Revise DOC-07 in `src/data/documents/batch2.ts`
Update the `DOC_07` template string with the new "REVISED" content:

- Change the SUBJECT block to keep the same wording (already matches).
- Replace item **5** in the numbered list:
  - From: `5. The pregnancy surviving the incident.`
  - To: `5. The fetus missing upon medical examination. / The uterus is empty. No live birth has / occurred. The pregnancy has been terminated / through surgical removal during the incident.`
- Keep items 1–4 and 6 unchanged.
- Insert a new section **ADDITIONAL OBSERVATIONS** between `THE WITNESS PATTERN` and `WHAT I AM ASKING`, containing the Kapoor/Mehrotra/Anand non-matching blood and the repeated dissociative statement ("He ate my baby while it was still moving."), plus the note that FIRs classified it as trauma-induced delusion.
- Leave the handwritten V.A. footer unchanged.

No other documents or files in `src/data/documents/` will be touched.

## 3. Add placeholder images to the Treasures sections
`src/components/act1/TreasuresBox.tsx` reads from `src/data/treasures.ts`, which globs `src/assets/treasures/section-a/` and `section-b/`. Currently both folders are empty (only README.md), so the section shows the "drop images…" empty state.

Approach:
- Generate 3 placeholder images per section (6 total) using the agent image tool, saved as:
  - `src/assets/treasures/section-a/placeholder-01.jpg` … `placeholder-03.jpg`
  - `src/assets/treasures/section-b/placeholder-01.jpg` … `placeholder-03.jpg`
- Style: soft scrapbook/polaroid-friendly placeholder cards (warm pastel backgrounds, simple "treasure" iconography). These render through the existing polaroid/lightbox UI without any code changes.

No changes to `treasures.ts` or `TreasuresBox.tsx` are needed — the glob picks them up automatically.

## Technical notes
- DOC-07 lives in `export const DOC_07 = \`…\`;` at lines ~321–479 of `src/data/documents/batch2.ts`. Edit performed via a single `apply_patch` hunk targeting items 5/6 and the new ADDITIONAL OBSERVATIONS block.
- After extraction, the auto-generated `src/routeTree.gen.ts` will be regenerated by the Vite plugin on dev start; no manual edit.
- Quick verification: confirm `rg "fetus missing upon medical examination" src/data/documents/batch2.ts` matches and the treasures folders each contain 3 image files.
